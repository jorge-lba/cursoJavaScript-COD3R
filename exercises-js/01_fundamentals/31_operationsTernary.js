/**
 * Operadores Ternarios.
 * Operadores ternários são compostos por 3 operações
 */

 const result = note => note >= 7 ? 'Aprovado' : 'Reprovado';

 console.log(result(7.1));
 console.log(result(6.7));
 