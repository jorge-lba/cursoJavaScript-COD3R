/**
 * Função é uma das coisas mais importantes do javaScript,
 * quase tudo é uma função, abaixo podemos ver que até
 * mesmo um objeto é uma função.
 */
console.log(typeof Object);

// Uma classe tambem é uma função.
class Produto{
};
console.log(typeof Produto);